# API base URL (no trailing slash). Local dev: localhost or LAN IP. Production: terraform output api_url
EXPO_PUBLIC_API_URL=http://localhost:3000

# Expo / EAS project UUID — required for push tokens on device builds
EXPO_PUBLIC_PROJECT_ID=
